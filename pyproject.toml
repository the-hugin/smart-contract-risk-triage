[project]
name = "smart-contract-risk-triage"
version = "0.3.0"
description = "Passive multi-chain smart-contract triage with funds-at-risk prioritization."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "smart-contract-risk-triage contributors" }]
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
